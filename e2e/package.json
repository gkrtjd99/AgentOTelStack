{
  "name": "e2e-runner",
  "version": "2.1.0",
  "private": true,
  "description": "Make-managed browser journeys for the sample app and canonical Go dashboard. Generates telemetry the agent can observe.",
  "scripts": {
    "test": "playwright test",
    "test:app": "playwright test journey.spec.js",
    "test:dashboard": "playwright test dashboard.spec.js",
    "install-browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
