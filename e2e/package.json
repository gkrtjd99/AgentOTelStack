{
  "name": "e2e-runner",
  "version": "2.0.1",
  "private": true,
  "description": "Browser UI journey against the sample app. Generates telemetry the agent can observe.",
  "scripts": {
    "test": "playwright test",
    "install-browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
