apiVersion: 1

providers:
  - name: local-observability
    orgId: 1
    folder: ObservabilityStack
    type: file
    disableDeletion: false
    allowUiUpdates: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/dashboards
